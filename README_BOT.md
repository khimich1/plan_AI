# 🤖 Telegram Bot для расчёта дорожек ПБ

Бот для автоматизации расчёта и визуализации планов раскладки плит дорожек.

## 🚀 Быстрый старт

### 1. Установите зависимости:
```bash
pip install -r requirements.txt
```

### 2. Настройте токен бота:
- Получите токен у @BotFather в Telegram
- Откройте файл `bot.env`
- Замените `1234567890:ABCDEFGHIJKLMNOPQRSTUVWXYZ123456789` на ваш токен

### 3. Запустите бота:
```bash
python bot_main.py
```

## 📋 Команды бота

- `/start` - главное меню и приветствие
- `/build_plan` - построить план дорожки
- `/help` - справка по командам
- `/stats` - статистика проекта

## 🔧 Что делает бот

✅ **Строит планы раскладки плит** - использует ваш существующий код `visualize_kz_plan.py`  
✅ **Рассчитывает стоимость** - берёт цены из базы данных `pb.db`  
✅ **Создаёт файлы** - PNG, PDF, XLSX, CSV  
✅ **Отправляет результаты** - прямо в Telegram  
✅ **Показывает статистику** - количество созданных файлов  

## 📁 Структура файлов

```
├── bot_main.py          # Главный файл бота
├── bot_handlers.py      # Обработчики команд
├── bot_config.py        # Настройки и пути
├── bot.env              # Токен бота (НЕ КОММИТИТЬ!)
├── requirements.txt     # Зависимости Python
├── visualize_kz_plan.py # Ваш существующий код
├── price_db.py          # Работа с базой данных
├── pb.db                # База данных цен
├── Визуализация_Раскладки/ # Результаты расчётов
└── банк знаний/         # Прайсы и документы
```

## 🎯 Как получить токен бота

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Мой Бот ПБ")
4. Введите username бота (например: "my_pb_bot")
5. Скопируйте токен и вставьте в `bot.env`

## ⚠️ Важные замечания

- **НЕ КОММИТЬТЕ** файл `bot.env` в Git!
- Убедитесь, что папка `Визуализация_Раскладки` существует
- Проверьте, что файл `pb.db` содержит данные о ценах

## 🐛 Решение проблем

**Ошибка "BOT_TOKEN не найден":**
- Проверьте файл `bot.env`
- Убедитесь, что токен скопирован правильно

**Ошибка импорта модулей:**
- Установите зависимости: `pip install -r requirements.txt`
- Проверьте, что все файлы на месте

**Бот не отвечает:**
- Проверьте интернет-соединение
- Убедитесь, что токен действительный
- Посмотрите логи в консоли

## 📞 Поддержка

Если что-то не работает, проверьте:
1. Все файлы созданы
2. Зависимости установлены
3. Токен бота правильный
4. Папки существуют

Удачи! 🚀
