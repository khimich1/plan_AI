# 📋 Инструкция по запуску бота

## 🚀 Быстрый запуск

### Способ 1: Двойной клик (если настроено)
Просто дважды кликните на файл `start_bot.sh` в файловом менеджере.

### Способ 2: Через терминал
Откройте терминал и выполните:

```bash
cd "/home/username/Рабочий стол/my py/plan_AI"
./start_bot.sh
```

## 🛑 Остановка бота

### Способ 1: Через скрипт
Дважды кликните на файл `stop_bot.sh` или выполните в терминале:

```bash
./stop_bot.sh
```

### Способ 2: Вручную
В окне терминала, где запущен бот, нажмите `Ctrl+C`

## 📁 Где хранятся логи?

Все логи сохраняются в папке `logs/` в корне проекта.

Каждый запуск создаёт новый файл лога с датой и временем:
- Например: `logs/bot_20250117_140530.log`

## ⚙️ Что делает скрипт запуска?

1. ✅ Проверяет наличие виртуального окружения (создаёт, если нет)
2. ✅ Устанавливает зависимости (если нужно)
3. ✅ Проверяет наличие необходимых файлов
4. ✅ Создаёт папку для логов
5. ✅ Запускает бота с логированием
6. ✅ Показывает все сообщения в консоли
7. ✅ Сохраняет логи в файл
8. ✅ Не закрывает окно при ошибке (ждёт нажатия Enter)

## ❓ Что делать, если что-то не работает?

1. Проверьте, что файл `bot.env` существует и содержит токен бота
2. Посмотрите логи в папке `logs/` - там будет подробная информация об ошибках
3. Убедитесь, что у скриптов есть права на выполнение:
   ```bash
   chmod +x start_bot.sh
   chmod +x stop_bot.sh
   ```

## 💡 Полезные команды

Проверить, запущен ли бот:
```bash
ps aux | grep bot_main
```

Посмотреть последний лог:
```bash
ls -lt logs/ | head -2
```

