# 💾 Сохранение КП в базу данных

## Как это работает

После создания коммерческого предложения через бота вы увидите предложение сохранить КП в базу данных.

### Шаг 1: Создание КП
Создайте КП через кнопку **"Коммерческое предложение PDF"** в боте:
1. Введите имя менеджера
2. Введите имя клиента
3. Отправьте список плит
4. Укажите процент скидки
5. Выберите условия поставки и оплаты

### Шаг 2: Предложение сохранить
После генерации файлов появится сообщение:
```
💾 Хотите сохранить это КП в базу данных?

Если сохраните, вы сможете отслеживать статус выполнения заказа.
```

С двумя кнопками:
- **💾 Сохранить в БД**
- **❌ Не сохранять**

### Шаг 3: Указание сроков (если выбрали "Сохранить")
После нажатия кнопки **"Сохранить в БД"** бот попросит указать сроки:
```
📅 Укажите сроки выполнения:
(Например: '14 дней', '2 недели', '01.02.2024')
```

Введите сроки в свободной форме, например:
- `14 дней`
- `2 недели`
- `01.02.2024`
- `до конца месяца`

### Шаг 4: Подтверждение
После ввода сроков КП сохраняется в базу данных и вы видите подтверждение:
```
✅ КП успешно сохранено в базу данных!

📋 Информация о КП:
  • Номер КП: 1
  • Дата: 19.12.2025
  • Клиент: ООО Клиент
  • Менеджер: Иванов И.И.
  • Сумма: 125,000.00 ₽ (с НДС)
  • Сроки: 14 дней
  • Статус: в работе

💡 Вы можете отслеживать статус этого КП в базе данных.
```

## Что сохраняется в БД

### База данных: `plita.db`
Расположена в корне проекта.

### Таблицы:

#### 1. `KP_offers` - Основная информация о КП
- Порядковый номер КП (автоматически)
- Дата создания
- Имя клиента
- Имя менеджера
- Процент скидки
- Сумма без НДС
- Сумма НДС
- Общая сумма
- Условия поставки
- Условия оплаты
- Сроки выполнения

#### 2. `kp_plates` - Плиты в КП
- Название плиты (например: "ПБ 78-12-8п")
- Длина, ширина, класс нагрузки
- Количество штук
- Вес одной плиты и общий вес
- Цена за единицу (со скидкой)

#### 3. `kp_files` - Файлы XLSX
- Сам файл XLSX (хранится как двоичные данные)
- Путь к файлу

#### 4. `kp_meta` - Метаданные
- Статус КП:
  - `в работе` (по умолчанию)
  - `выполнено`
  - `отклонено`
  - `в ожидании`

## Работа с базой данных через код

### Импорт модуля
```python
from core import kp_db
```

### Получить КП по номеру
```python
kp = kp_db.get_kp_by_id(1)
print(f"Клиент: {kp['customer_name']}")
print(f"Сумма: {kp['total_amount']} ₽")
print(f"Статус: {kp['status']}")
```

### Получить все КП в работе
```python
all_kp = kp_db.get_all_kp_by_status('в работе')
for kp in all_kp:
    print(f"КП № {kp['kp_id']} - {kp['customer_name']}")
```

### Обновить статус КП
```python
kp_db.update_kp_status(1, 'выполнено')
```

### Получить файл XLSX из БД
```python
# Получить файл как bytes
xlsx_data = kp_db.get_xlsx_file(1)

# Или сохранить файл на диск
kp_db.get_xlsx_file(1, output_path='/путь/к/файлу.xlsx')
```

## Пример полного цикла

1. **Создание КП**: через бота создаёте КП для клиента
2. **Сохранение**: сохраняете КП в БД со сроками "14 дней"
3. **Отслеживание**: в БД статус "в работе"
4. **Выполнение**: после выполнения заказа меняете статус на "выполнено"
5. **Анализ**: можете получить список всех КП, отфильтровать по статусу, клиенту и т.д.

## Технические детали

### Файлы проекта
- `core/kp_db.py` - модуль для работы с БД
- `bot/handlers/commercial.py` - обработчики создания КП с сохранением в БД
- `bot/states.py` - состояния FSM для диалогов
- `bot/keyboards.py` - клавиатуры бота
- `plita.db` - файл базы данных SQLite (создаётся автоматически)

### Автоматическое создание БД
База данных создаётся автоматически при первом вызове любой функции модуля `kp_db`.
Не нужно создавать таблицы вручную.

### Безопасность
- Файлы БД включены в `.gitignore` и не попадут в репозиторий
- Все данные хранятся локально на вашем сервере

## Полезные SQL-запросы

Если нужно работать с БД напрямую через SQL:

```sql
-- Все КП в работе
SELECT * FROM KP_offers 
JOIN kp_meta ON KP_offers.kp_id = kp_meta.kp_id
WHERE kp_meta.status = 'в работе';

-- КП по клиенту
SELECT * FROM KP_offers 
WHERE customer_name LIKE '%Клиент%';

-- Сумма всех КП за период
SELECT SUM(total_amount) FROM KP_offers 
WHERE creation_date >= '01.01.2024';
```

## Устранение проблем

### БД не создаётся
Проверьте права на запись в папку проекта:
```bash
ls -la plita.db
```

### Ошибка при сохранении
Проверьте логи бота:
```bash
tail -f logs/bot_*.log
```

### Файл XLSX не сохраняется
Убедитесь, что файл существует на момент сохранения:
```python
import os
print(os.path.exists('/путь/к/файлу.xlsx'))
```

## Дальнейшие улучшения

Возможные доработки:
- [ ] Веб-интерфейс для просмотра КП
- [ ] Экспорт статистики в Excel
- [ ] Автоматические напоминания о приближающихся сроках
- [ ] История изменений статусов
- [ ] Комментарии к КП
- [ ] Прикрепление дополнительных файлов

---

💡 **Совет**: Регулярно делайте резервные копии файла `plita.db`:
```bash
cp plita.db plita_backup_$(date +%Y%m%d).db
```
