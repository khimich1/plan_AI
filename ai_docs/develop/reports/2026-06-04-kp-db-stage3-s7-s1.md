# kp_db stage 3 — S7 + S1/A7 (2026-06-04)

Closes audit items **S7** and **S1/A7** (kp_db scope) from `2026-06-03-core-kp-db-audit.md`.

Prior: [stage 1](2026-06-04-kp-db-stage1-remediation.md), [stage 2 S4](2026-06-04-kp-db-stage2-s4-remove-2-55.md).

## S7 — destructive reset guard

- New [`core/destructive_db_guard.py`](../../../core/destructive_db_guard.py): `require_destructive_db_reset()`, `DestructiveDbOperationBlocked`.
- [`clear_all_plates_data`](../../../core/kp_db.py), [`clear_all_kp`](../../../core/kp_db.py) call guard at entry.
- Web: [`app/api/v1/endpoints/admin.py`](../../../app/api/v1/endpoints/admin.py) → HTTP **403** on blocked reset.
- Bot: [`bot/handlers/admin.py`](../../../bot/handlers/admin.py) → user-visible message.

**Env** (see [`.env.example`](../../../.env.example)):

- `APP_ENV=production` blocks `clear_all_*` by default.
- `ALLOW_DESTRUCTIVE_DB_RESET=1` explicitly allows reset in production.

## S1/A7 — agent NDJSON gate (kp_db hot path)

- Extended [`core/debug_paths.py`](../../../core/debug_paths.py): `kp_db_agent_debug_active()`, `append_agent_debug_log()`, `append_agent_debug_session()`.
- [`core/kp_db.py`](../../../core/kp_db.py) `move_plates_to_completed` / `find_one_row`: all former `open(debug_logs/...)` writes go through gated helpers.
- `_debug_session_write` delegates to `append_agent_debug_session`.

**Enable debug:**

- `APP_DEBUG=true` (settings `app_debug`), or
- `KP_DB_AGENT_DEBUG=1` (spисание only).

## Tests

- `tests/test_destructive_db_guard.py` — production block + flag
- `tests/test_kp_db_agent_debug_log.py` — no file write when disabled

```
pytest tests/test_destructive_db_guard.py tests/test_kp_db_agent_debug_log.py \
  tests/test_admin_service.py tests/test_kp_db_move_plates_to_completed.py \
  tests/test_production_completion_service.py tests/test_plan_consistency.py -v
```

**46 passed.**

## Not in scope

- Bot/commercial/production handler NDJSON (stage 3b)
- A1/A2 decomposition, A4 init_schema once
- Bot `kp_plate_id` follow-up after S4

## Next suggested step

- Stage 3b: gate debug in `bot/handlers/production_*.py` / `commercial.py`, or
- Bot `kp_plate_id` on complete_day, or
- A4: single `init_schema` on app startup
