# Сводный отчёт аудита: `viz_modules/`

**Дата:** 2026-05-14  
**Область:** `viz_modules/`  
**Провели:** senior-reviewer (архитектура), security-auditor (безопасность), reviewer (качество кода)

## Краткое резюме

Отчёт объединяет выводы по архитектуре, безопасности и качеству кода для пакета `viz_modules/`. Общая оценка состояния — **4.0/10** по формуле audit-workflow: старт с 10; минус **2** за каждую critical-находку (суммарный штраф по critical не более **6**); минус **0.5** за каждую high (не более **3** в сумме); минус **0.1** за каждую medium (не более **1** в сумме). Здесь: одна critical (−2), шесть high (−3 с ограничением), десять medium (−1 с ограничением) → **4.0/10**.

**Рекомендация:** устранить **единственную critical** проблему до того, как опираться на пути разметки в `builder.py`. Находку **high** по валидации путей **[S1]** считать **P1**, если какой-либо вызывающий код может передать **контролируемые пользователем** пути в файловой системе.

## Итоги по серьёзности (по столбцам)

| Серьёзность | Архитектура | Безопасность | Качество кода | **Всего** |
|-------------|-------------|--------------|---------------|-----------|
| Critical | 1 | 0 | 0 | **1** |
| High | 3 | 1 | 2 | **6** |
| Medium | 4 | 2 | 4 | **10** |
| Low | 3 | 2 | 3 | **8** |

## Critical

| ID | Источник | Кратко |
|----|----------|--------|
| **[A1]** | Архитектура | Недостижимая дублирующая ветка в `viz_modules/layout_sequence/builder.py` (примерно строки 273–925): после `if OPT_CASCADING_PLAN and OPT_CASCADING_PLAN.get("primary_cuts"):` примерно на 266–271 выполняется делегирование в `_build_sequence_from_plan` и **`return`**, затем на ~273 снова то же условие — легаси-конвейер **никогда не выполняется** (~650 строк «мёртвого» кода или скрытый баг в условиях). |
| **[Q1]** | Качество кода | Та же первопричина, что у **[A1]**: после `return` в `builder.py` остаётся недостижимая дублирующая ветка — сотни строк по сути не используются по сравнению с путём через `from_plan.py`. |

*Примечание: **[A1]** и **[Q1]** описывают один дефект с точки зрения архитектуры и сопровождаемости; исправление закрывает оба пункта.*

## High

| ID | Источник | Кратко |
|----|----------|--------|
| **[A2]** | Архитектура | «Божественный» модуль `viz_modules/procurement.py` (~1888 строк) — разбить по сценариям использования. |
| **[A3]** | Архитектура | Чрезмерная оркестрация в `layout_sequence/from_plan.py` — выделить фазы. |
| **[A4]** | Архитектура | Жёсткая связка с глобальным состоянием `core`/SQLite — внедрять снимки состояния и фасады. |
| **[S1]** | Безопасность | Непроверенные пути в ФС попадают в `sqlite3.connect` и запись NDJSON-трейсов (`deps.py`, `builder.py`, `price_utils.py`, `debug_trace.py`, `helpers.py`, `from_plan.py`). **Если пути задаёт пользователь:** риск обхода контроля доступа / path traversal. |
| **[Q2]** | Качество кода | `_build_sequence_from_plan_impl` — крайне высокая сложность и вложенность; нужны характеризационные тесты и декомпозиция. |

## Medium

| ID | Источник | Кратко |
|----|----------|--------|
| **[A5]** | Архитектура | Дублирование перебора ширин плит в `from_plan.py` и `builder.py` — вынести в общий хелпер. |
| **[A6]** | Архитектура | Отладка/трейсы вплетены в основные пути; широкие `except` вокруг диагностики — оборачивать флагами, единообразно использовать что-то вроде `SequenceTracer`. |
| **[A7]** | Архитектура | Тяжёлый `viz_modules/__init__.py` с реэкспортом, в т.ч. приватных drawing API — предпочесть ленивые экспорты. |
| **[A8]** | Архитектура | Паттерн самоимпорта в `_dispatch_agent_seq_debug` — внедрять через `LayoutSequenceDeps`. |
| **[S2]** | Безопасность | Чувствительные коммерческие/расчётные данные в NDJSON-трейсах — риск утечки при копировании/шаринге логов. |
| **[S3]** | Безопасность | Неоднозначный выбор XLSX в `price_utils.load_price_table_from_xlsx` — эвристика берёт первое совпадение — целостность / confused deputy. |
| **[Q3]** | Качество кода | DRY: дублируются циклы по плитам; `_warn_missing_reinforcement` продублирован в `from_plan.py` и `builder.py`. |
| **[Q4]** | Качество кода | Широкие `except Exception: pass` вокруг I/O трейсов в `builder.py`, `helpers.py`, `from_plan.py`. |
| **[Q5]** | Качество кода | В `procurement.py` неравномерная диагностика (`print` против широких `except`). |
| **[Q6]** | Качество кода | Дыра в тестах: большая поверхность `procurement.py` покрыта слабо; «мёртвая» ветка **[A1]/[Q1]** не проверяется. |

## Low

| ID | Источник | Кратко |
|----|----------|--------|
| **[A9]** | Архитектура | `print` в `procurement.py`, `visualization_drawing.py` — перейти на структурированное логирование. |
| **[A10]** | Архитектура | Артефакты `_extract_*.txt` внутри пакета — перенести в docs/fixtures. |
| **[A11]** | Архитектура | **Плюс:** `deps.py`, contextvars в `debug_trace`, внедрение данных по армированию, узкий `secondary_ops` — конструктивные приёмы. |
| **[S4]** | Безопасность | Проглоченные ошибки при диагностическом I/O — слабый мониторинг/наблюдаемость. |
| **[S5]** | Безопасность | Диагностика через `print` — риск утечки в stdout/логи эксплуатации. |
| **[S6]** *(примечание)* | Безопасность | В области аудита своей аутентификации нет; границы должны обеспечивать вышестоящие сервисы (authN/authZ). |
| **[Q7]** | Качество кода | Слабая типизация в `visualization_drawing.py` (`float=None`, дефолты `list=None`). |
| **[Q8]** | Качество кода | Шум в пакете из файлов `_extract_*.txt` (согласовать с **[A10]**). |
| **[Q9]** | Качество кода | `__init__.py` реэкспортирует приватные `_draw_*` в `__all__` (согласовать с **[A7]**). |

## Матрица приоритетов (предложение)

| Приоритет | Обоснование | Находки |
|-----------|-------------|---------|
| **P0** | Блокирует доверие к корректности выполнения разметки и потоку в `builder.py` | **[A1]**, **[Q1]** |
| **P1** | Эксплуатация или политика, если входы извне; большой долг по сопровождению/безопасности | **[S1]**, **[A2]**, **[A3]**, **[Q2]** |
| **P2** | Связность, тестируемость, дубли, диагностика | **[A4]**–**[A8]**, **[Q3]**–**[Q6]**, **[S2]**–**[S3]** |
| **P3** | Гигиена, логирование, артефакты, типы, поверхность `__init__` | **[A9]**–**[A10]**, **[Q7]**–**[Q9]**, **[S4]**–**[S5]** |

## Сквозные темы

1. **Поток управления в `builder.py`:** доминирует critical **[A1]/[Q1]**; при исправлении нужны тесты, которые фиксируют, какая ветка выполняется для типичных форм `OPT_CASCADING_PLAN`.
2. **Файловая система и трейсы:** **[S1]**, **[S2]**, **[Q4]** вместе означают необходимость валидации/ограничения входных путей и редактирования/изоляции NDJSON.
3. **Размер модулей и оркестрация:** **[A2]**, **[A3]**, **[Q2]** указывают на нарезку `procurement.py` и фаз в `from_plan.py` с явными интерфейсами.
4. **Публичная поверхность пакета:** **[A7]**, **[Q9]** — уменьшить ранний публичный API для приватных drawing-хелперов.

## Следующие шаги

1. **P0:** Удалить или объединить дублирующую ветку `OPT_CASCADING_PLAN` в `builder.py`, чтобы поведение было явным; добавить регрессионные тесты для планового vs легаси-пути.
2. **Проверить P1 для [S1]:** инвентаризация вызовов с параметрами путей; при пользовательском вводе — allowlist, нормализация к доверенному корню или только серверные пути из конфигурации для БД/трейсов.
3. **Снизить сложность:** декомпозировать `_build_sequence_from_plan_impl` и ввести характеризационные тесты (**[Q2]**).
4. **Рефакторинг горячих точек:** наметить границы разбиения `procurement.py` (**[A2]**) и выделения фаз из `from_plan.py` (**[A3]**).
5. **Шов для тестов:** внедрять данные через фасады вместо SQLite/глобалов (**[A4]**).
6. **Укрепить вспомогательные пути:** **[S2]/[S3]** (содержимое трейсов, выбор XLSX) и объединить дублирующие хелперы (**[A5]/[Q3]**).
7. **Наведение порядка:** заменить `print` на логирование (**[A9]/[S5]/[Q5]**), перенести `_extract_*.txt` (**[A10]/[Q8]**), сузить экспорты в `__init__.py` (**[A7]/[Q9]**).

---

## Связанные файлы (по ссылкам ревьюеров)

Архитектура и качество ссылаются на: `viz_modules/layout_sequence/builder.py`, `viz_modules/layout_sequence/from_plan.py`, `viz_modules/procurement.py`, `viz_modules/__init__.py`, `viz_modules/visualization_drawing.py`, а также модули helpers/deps/trace, перечисленные в **[S1]**.

---

**Сохранено (абсолютный путь):** `c:\Users\Роман\Desktop\Шишов\ai_docs\develop\audits\2026-05-14-viz-modules-audit.md`
