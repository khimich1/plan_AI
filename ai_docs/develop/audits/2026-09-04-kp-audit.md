# Аудит: kp (конструктор КП)

**Дата**: 2026-09-04
**Скоуп**: конструктор КП / commercial / offers / archive — backend (`app/api/v1/endpoints/commercial.py`, `production.py`, `archive.py`), сервисы КП, frontend мастера и архива
**Реестр**: [FINDINGS.md](./FINDINGS.md)
**Прогон 0**: [2026-08-28-full-registry-sweep-audit.md](./2026-08-28-full-registry-sweep-audit.md) (сверка реестра). Более ранний именной kp-отчёт: [2026-08-13-kp-append-delivery-schedule-audit.md](./2026-08-13-kp-append-delivery-schedule-audit.md) (IDs там не из текущего реестра).

## Дельта

| ID | Было | Стало | Суть |
|----|------|-------|------|
| A3 | resolved | resolved | Facade `CommercialWorkflowService` — 47 delegate methods, lifecycle вынесен; 780 строк (рост не переоткрывает) |
| A4 | open | open | `production.py` wc=642, 26 routes; day-documents и SGP CRUD обходят ProductionService |
| A5 | open | open | Raw SQL в sgp / delivery_schedule / kp_readiness без repository-слоя |
| A11 | open | open | `archive_service.py` 862 строк (+50), lazy-coupling к трём сервисам |
| A12 | open | open | `useCommercialOfferWizard.ts` 517 строк (+64), 28 useMutation |
| A15 | by-design | by-design | `owner_user_id` вне policy доступа — подтверждено ADR |
| S1 | by-design | by-design | Общий архив КП — не IDOR |
| S5 | open | open | OCR шлёт изображения во внешние LLM при `OCR_EXTERNAL_ENABLED=true` |
| S8 | open | open | `str(exc)` в HTTP через `raise_unprocessable_client_error` (kp: production, archive) |
| S9 | open | open | CSRF парсит multipart до проверки токена (kp: commercial upload) |
| S15 | open | open | Draft в sessionStorage |
| Q1 | resolved | resolved | Product-type pipeline в `product_draft_config` + handler |
| Q2 | resolved | resolved | Copy-paste HTTP-обработчиков поглощён A4 runners |
| Q4 | open | open | Пять копий `build_*_preview_metadata` в `commercial_draft_service.py` |
| Q5 | resolved | resolved | `commercial_plate_resolve.py` — единый resolve wide/unpriced |
| Q7 | open | open | Product-type duplication на фронте |
| Q8 | open | open | God-hook `useCreatePlanWizardState` wc=724 |
| Q9 | open | open | `OfferDetailsDrawer.tsx` 1066 строк (+165) |
| Q10 | open | open | 9 routes в `production.py` без `response_model` |
| Q11 | open | open | `preview: Any` / `dict[str, Any]` в schemas |
| Q12 | open | open | ArchiveService глотает частичные сбои (`except Exception → None`) |
| Q16 | open | open | `/parse` без `response_model` |
| Q18 | open | open | 3/9 suppressions `react-hooks/exhaustive-deps` в kp-подмножестве |
| Q22 | — | open | **NEW** — дублирование `is_*_draft` в трёх модулях |

- Закрыто: 0
- Открыто (подтверждено): 18
- Новое: 1 (Q22)
- Не воспроизвелось: 0

**Открытые P0 / P1 в скоупе**: 0 / 2 (A4, A5)

## Действия (максимум 8)

1. **[A4]** Вынести бизнес-логику из `production.py` в `ProductionService`: `build_plan_from_filters`, day-documents (`generate_day_*`), SGP CRUD, `_date_range_inclusive` — thin controller.
2. **[A5]** Repository-слой для `sgp_service`, `delivery_schedule_service`, `kp_readiness_service` вместо inline `cur.execute` / CTE.
3. **[A11]** Разделить `ArchiveService` (862 строк) на ArchiveReadService / ArchiveMutationService / ArchiveCapacityService; убрать lazy-coupling `:124`, `:140`, `:477`.
4. **[A12]** Разделить `useCommercialOfferWizard.ts` (517 строк, 28 mutations) на `useWizardProductMutations` + `useWizardDraftMutations`.
5. **[Q9]** Декомпозировать `OfferDetailsDrawer.tsx` (1066 строк) — drawer разросся на +165 строк с прошлого прогона.
6. **[Q4]** Обобщить пять `build_*_preview_metadata` в `commercial_draft_service.py` (820 строк).
7. **[Q12]** Не глотать исключения в archive capacity/move (`:473–487`, `:541–542`, `:579–580`, `:783–785`); сохранить re-raise в `move_to_production`.
8. **[Q22]** DRY для `is_*_draft`: канон в `commercial_calculation_service.py:28–45`, убрать копию в `commercial_export_service.py:178–191`.

## Открытые P0 / P1

### [A4] Толстый API-слой production.py
**Статус**: open
**Улика**: `app/api/v1/endpoints/production.py` wc=642, 26 routes; `build_plan_from_filters` `:104–167`; day-documents `:400–488` вызывают `generate_day_*` напрямую, минуя ProductionService; SGP CRUD `:501–574`; `_date_range_inclusive` `:594–600`
**Зачем**: Роутер смешивает HTTP, валидацию и доменную логику; day-documents и SGP — регрессия после декомпозиции `commercial.py`; усложняет тестирование и повторное использование.

### [A5] Сервисы обходят repository, raw SQL
**Статус**: open
**Улика**: `app/services/sgp_service.py:78–89` (`cur.execute` SELECT completed_plates); `app/services/delivery_schedule_service.py:494–500` (SELECT FROM kp_plates); `app/services/kp_readiness_service.py:49–59` (CTE WITH identities); нет *sgp* / *delivery* / *readiness* repos
**Зачем**: Raw SQL в сервисах обходит слой репозиториев, дублирует запросы, усложняет миграцию схемы и unit-тесты.

## Приложение

### Архитектура (Medium)

**[A11] ArchiveService god-orchestrator** — `archive_service.py` вырос до 862 строк (было 812), 30 методов. Lazy-coupling: `:124` CommercialWorkflowService, `:140` KpReadinessService, `:477` SgpService. Предложение: ArchiveReadService / ArchiveMutationService / ArchiveCapacityService.

**[A12] Frontend god-hook мастера КП** — `useCommercialOfferWizard.ts` 517 строк (было 453), 28 `useMutation` (`:72–471`). Предложение: `useWizardProductMutations` + `useWizardDraftMutations`.

**[A15] by-design** — `offer_access.py:26–32`; `owner_user_id` не входит в policy доступа КП (ADR offer-access-policy.md).

### Безопасность

**[S1] by-design** — общий архив КП для менеджеров; `offer_access.py:26–32`, ADR offer-access-policy.md. Не IDOR.

**[S5] OCR → внешние LLM** — при `OCR_EXTERNAL_ENABLED=true`: `core/ocr/providers/openai.py:355–367`, `pipeline.py:50–53`, `commercial_upload_validation.py:60–66`, `commercial.py:102–104`, `:328–329`; policy покрыт `tests/test_commercial_ocr_policy.py`.

**[S8] Утечка деталей ошибок (kp evidence)** — `production.py:140`, `:190`, `:196`, `:640`; `archive.py:296`, `:324` — `str(exc)` клиенту через `raise_unprocessable_client_error`.

**[S9] CSRF multipart (kp evidence)** — `csrf.py:42–47` парсит multipart до токена; `commercial.py:194–202`, `:321–329`; frontend `httpClient.ts:130–132` шлёт header.

**[S15] Draft в sessionStorage** — `draftStorage.ts:3`, `:18–19` — XSS-вектор черновика.

**S3 (platform, kp-touchpoint)** — in-process OCR rate limit: `commercial_upload_validation.py:23–45`, `:141–142`. Scope остаётся platform.

### Качество

**[Q4]** Пять `build_*_preview_metadata` — `commercial_draft_service.py:270`, `:340`, `:410`, `:480`, `:550`; файл 820 строк.

**[Q7]** Product-type duplication — `commercialOfferApi.ts:145–243` + wizard `:81–275`.

**[Q8]** God-hook — `useCreatePlanWizardState.ts` wc=724; `eslint-disable` `:236`.

**[Q9]** God-component — `OfferDetailsDrawer.tsx` 1066 строк (было 901).

**[Q10]** Слабая типизация production API — 9 routes без `response_model`: `:86–90`, `:94–99`, `:207–212`, `:253–258`, `:308–312`, `:351–357`, `:491–496`, `:577–581`, `:585–590`.

**[Q11]** `preview: Any` — `commercial_draft_service.py:192`, `:273`, `:343`, `:413`, `:483`, `:553`; `schemas/commercial.py:311–318` `dict[str, Any]`.

**[Q12]** ArchiveService скрывает сбои — `:473–487`, `:541–542`, `:579–580`, `:783–785` swallow; `move_to_production` `:243–247` re-raises.

**[Q16]** `/parse` без `response_model` — `commercial.py:157`.

**[Q18]** Suppressions exhaustive-deps — kp subset 3/9: `MoveToProductionDialog.tsx:66`, `DeliveryScheduleDialog.tsx:92`, `useCreatePlanWizardState.ts:236`; нет suppressions в `commercial-offer/`.

**[Q22] NEW — дублирование is_*_draft** — канон: `commercial_calculation_service.py:28–45`; копия: `commercial_export_service.py:178–191`; делегат: `wizard_step_service.py:52–65` → calculation.

### Подтверждённо resolved (без переоткрытия)

- **A3** — facade `commercial_workflow_service.py:38–66` (47 delegates), `:629–636` hydrate → draft_lifecycle; извлечены commercial_order_identity, commercial_plate_resolve, product_draft_handler, commercial_draft_lifecycle (1001 строк); facade 780 строк.
- **Q1** — `product_draft_config` + `product_draft_handler`.
- **Q2** — runners `commercial.py:98–319`.
- **Q5** — `commercial_plate_resolve.py:66`, `:80`.

### Verified OK (не находки)

- **Logistics** — 403 + slim search без финансовых полей (`tests/test_logistics_api.py:454–489`, `:516–528`).
- **Draft path traversal** — guarded (`draft_store.py:51–74`, `commercial_draft.py:10–32`; тесты `test_commercial_web_flow.py:354–385`, `:697–719`).
- **Archive delete** — single-row + write access (`archive.py:260–264`).
- **Bulk reset** — `core/kp/offers_write.py:458`.
