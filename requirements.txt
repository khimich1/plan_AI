# Зависимости для веб-сервера: uvicorn app.main:app (FastAPI + общий пакет core/).
# SPA во frontend/: зависимости в frontend/package.json — ставятся в Dockerfile через npm ci, не через pip.
# Telegram-бот DEPRECATED (P5 WP1): опционально pip install -r requirements-bot.txt — только bot_archived/

fastapi==0.111.0
uvicorn>=0.30.0

pydantic==2.7.4
pydantic-settings==2.2.1
python-dotenv>=0.19.0
python-multipart>=0.0.9

filelock>=3.13.0

# Таблицы / прайсы / КП
pandas>=1.3.0
openpyxl>=3.0.0
xlrd>=2.0.1
python-docx>=0.8.11

# ГСМ: геокод Nominatim / OSRM (scripts/build_gsm_routes_map.py)
requests>=2.28.0
# ГСМ: декод plus-кодов в адресах АЗС (scripts/build_gsm_trip_feed.py)
openlocationcode>=1.0.1

# PDF и схемы раскладки
reportlab>=4.0.0
matplotlib>=3.5.0

# ILP-раскрой (в образе должен быть CBC: см. coinor-cbc в Dockerfile)
pulp>=2.6.0

# OCR загрузки КП через Vision (GigaChat основной, OpenAI fallback)
gigachat>=0.1.39
openai>=1.0.0
