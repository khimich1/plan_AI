# 🔧 Исправление проблемы с сохранением КП в БД

## Что было исправлено

### 1. Очищена тестовая база данных
Старая БД содержала тестовые данные из скрипта `test_kp_db.py`. Теперь БД очищена.

### 2. Добавлено логирование
Теперь в консоли бота будут видны сообщения:
```
[DEBUG] Данные сохранены в state:
  - Клиент: Боба
  - Менеджер: Биба
  - Скидка: 2.0%
  - Плит: 4
[DEBUG] Нажата кнопка 'Сохранить в БД'
[DEBUG] Переход к состоянию waiting_execution_terms
[DEBUG] Получены сроки: 14 дней
```

### 3. Исправлено состояние FSM
Callback-обработчик мог не срабатывать из-за активного состояния FSM. 
Теперь перед показом кнопок состояние очищается.

## Как проверить работу

### Шаг 1: Перезапустите бота

```bash
cd "/home/username/Рабочий стол/my py/plan_AI"
./stop_bot.sh
./start_bot.sh
```

Или если бот не запущен:
```bash
python3 run_bot.py
```

### Шаг 2: Создайте КП через бота

1. Нажмите **"Коммерческое предложение PDF"**
2. Введите имя менеджера (например: "Биба")
3. Введите имя клиента (например: "Боба")
4. Отправьте список плит
5. Укажите скидку (например: 2)
6. Выберите условия

### Шаг 3: После генерации файлов

Вы должны увидеть сообщение:
```
💾 Хотите сохранить это КП в базу данных?

Если сохраните, вы сможете отслеживать статус выполнения заказа.

[💾 Сохранить в БД]  [❌ Не сохранять]
```

### Шаг 4: Нажмите "Сохранить в БД"

Должно появиться:
```
✅ Сохраняю КП в базу данных...

📅 Укажите сроки выполнения:
(Например: '14 дней', '2 недели', '01.02.2024')
```

### Шаг 5: Введите сроки

Например: `14 дней`

### Шаг 6: Проверьте БД

Запустите скрипт просмотра БД:
```bash
python3 view_kp_db.py
```

Вы должны увидеть ваше КП с правильными данными:
```
✅ Найдено КП: 1

============================================================
📋 КП № 1
============================================================
📅 Дата создания: 19.12.2025
👤 Клиент: Боба
👨‍💼 Менеджер: Биба
💰 Скидка: 2.0%
💵 Сумма без НДС: ...
📊 НДС (20%): ...
💎 ИТОГО: ...
⏰ Сроки: 14 дней
📌 Статус: в работе

📦 Плиты в заказе (4 позиций):
  1. ПБ 56-3,2-8п
     • Количество: 4 шт
     ...
```

## Проверка логов

Если проблема осталась, посмотрите логи бота:

```bash
# Если бот запущен через start_bot.sh
tail -f logs/bot_*.log

# Или если бот запущен в терминале
# Просто смотрите вывод в консоли
```

Вы должны видеть сообщения вида:
```
[DEBUG] Данные сохранены в state:
  - Клиент: Боба
  - Менеджер: Биба
  - Скидка: 2.0%
  - Плит: 4
```

## Возможные проблемы и решения

### Проблема 1: Кнопки не появляются

**Причина:** Бот не перезапущен после изменений

**Решение:**
```bash
./stop_bot.sh
./start_bot.sh
```

### Проблема 2: После нажатия кнопки ничего не происходит

**Причина:** Callback-обработчик не срабатывает

**Решение:**
1. Проверьте логи — должно быть `[DEBUG] Нажата кнопка 'Сохранить в БД'`
2. Если сообщения нет, проблема в регистрации обработчика
3. Убедитесь, что файл `bot/handlers/commercial.py` изменён

### Проблема 3: Данные в БД неправильные

**Причина:** Сохраняются не те данные из state

**Решение:**
1. Посмотрите логи — там должны быть правильные данные из state
2. Если в логах данные правильные, но в БД неправильные — проблема в функции `save_kp_to_db`
3. Если в логах данные неправильные — проблема в функции `generate_all_documents`

### Проблема 4: В БД пусто

**Причина:** Не дошло до сохранения или ошибка при сохранении

**Решение:**
1. Проверьте логи на наличие ошибок
2. Проверьте, что БД создана: `ls -la plita.db`
3. Проверьте права на запись: `touch plita.db`

## Ручная проверка БД через SQL

```bash
sqlite3 plita.db

# В SQLite консоли:
.tables                          # Показать все таблицы
SELECT * FROM KP_offers;         # Показать все КП
SELECT * FROM kp_plates;         # Показать все плиты
SELECT * FROM kp_meta;           # Показать статусы
.quit                            # Выход
```

## Очистка БД (если нужно начать с нуля)

```bash
rm plita.db
# БД будет создана заново при первом сохранении
```

## Проверка правильности данных

После сохранения КП сравните:

### В XLSX файле:
- Клиент: Боба
- Менеджер: Биба (в ячейке A32)
- Скидка: 2.0%
- Плиты: ПБ 56-3,2-8п (4 шт), ПБ 56-8,6-8п (5 шт), и т.д.

### В БД (через `python3 view_kp_db.py`):
- Клиент: должен быть "Боба"
- Менеджер: должен быть "Биба"
- Скидка: должна быть 2.0%
- Плиты: должны совпадать с XLSX

## Файлы для отладки

- `view_kp_db.py` — просмотр содержимого БД
- `test_kp_db.py` — тестирование БД (НЕ запускайте после реального КП!)
- `ИСПРАВЛЕНИЕ_ПРОБЛЕМЫ_БД.md` — этот файл

## Если ничего не помогло

1. Удалите БД: `rm plita.db`
2. Перезапустите бота: `./stop_bot.sh && ./start_bot.sh`
3. Создайте КП заново
4. Смотрите логи в реальном времени
5. Присылайте логи для анализа

---

**Важно:** Не запускайте `test_kp_db.py` после создания реального КП — это добавит тестовые данные в БД!
