# 🚀 Быстрый старт: Сохранение КП в базу данных

## Что добавлено

После создания коммерческого предложения в боте теперь появляется предложение сохранить КП в базу данных со сроками выполнения.

## Как это работает (пошагово)

### 1. Создайте КП как обычно

Нажмите кнопку **"Коммерческое предложение PDF"** в боте и заполните все поля:
- Имя менеджера
- Имя клиента  
- Список плит
- Процент скидки
- Условия поставки и оплаты

### 2. Получите файлы КП

Бот отправит вам:
- 📄 PDF файл
- 📊 XLSX файл с формулами
- 📐 Схему раскладки
- 📊 Детальную разбивку

### 3. Предложение сохранить в БД

После отправки файлов появится сообщение:

```
💾 Хотите сохранить это КП в базу данных?

Если сохраните, вы сможете отслеживать статус выполнения заказа.

[💾 Сохранить в БД]  [❌ Не сохранять]
```

### 4. Если нажмёте "Сохранить в БД"

Бот попросит указать сроки:

```
✅ Сохраняю КП в базу данных...

📅 Укажите сроки выполнения:
(Например: '14 дней', '2 недели', '01.02.2024')
```

Напишите сроки в любом формате, например:
- `14 дней`
- `2 недели`
- `до 01.02.2024`
- `до конца месяца`

### 5. Подтверждение

После ввода сроков вы увидите:

```
✅ КП успешно сохранено в базу данных!

📋 Информация о КП:
  • Номер КП: 1
  • Дата: 19.12.2025
  • Клиент: ООО Клиент
  • Менеджер: Иванов И.И.
  • Сумма: 125,000.00 ₽ (с НДС)
  • Сроки: 14 дней
  • Статус: в работе

💡 Вы можете отслеживать статус этого КП в базе данных.
```

### 6. Если нажмёте "Не сохранять"

КП не будет сохранено в БД, и бот вернётся в главное меню.

## Что сохраняется в базе данных

База данных `plita.db` хранит:

### 📋 Основная информация
- Номер КП (автоматически)
- Дата создания
- Имя клиента
- Имя менеджера
- Процент скидки
- Суммы (без НДС, НДС, итого)
- Условия поставки и оплаты
- **Сроки выполнения** (то, что вы указали)
- Статус ("в работе", "выполнено", "отклонено", "в ожидании")

### 📦 Плиты в заказе
Для каждой позиции:
- Название плиты (например: "ПБ 78-12-8п")
- Размеры (длина, ширина, нагрузка)
- Количество штук
- Вес (одной плиты и общий)
- Цена за единицу (со скидкой)

### 📁 Файлы
- Сам файл XLSX (сохраняется внутри БД)
- Путь к файлу на диске

## Проверка работы

Запустите тестовый скрипт:

```bash
cd "/home/username/Рабочий стол/my py/plan_AI"
python3 test_kp_db.py
```

Вы должны увидеть:

```
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!
💡 База данных plita.db готова к работе!
```

## Где находятся файлы

- `plita.db` — база данных (в корне проекта)
- `core/kp_db.py` — модуль для работы с БД
- `bot/handlers/commercial.py` — обработчики с добавленным функционалом
- `СОХРАНЕНИЕ_КП_В_БД.md` — подробная документация
- `test_kp_db.py` — тестовый скрипт

## Полезные команды

### Просмотр базы данных

```bash
sqlite3 plita.db "SELECT * FROM KP_offers;"
```

### Резервная копия

```bash
cp plita.db plita_backup_$(date +%Y%m%d).db
```

### Просмотр всех КП в работе

```bash
sqlite3 plita.db "SELECT kp_id, customer_name, total_amount, execution_terms 
FROM KP_offers 
JOIN kp_meta ON KP_offers.kp_id = kp_meta.kp_id 
WHERE status='в работе';"
```

## Что дальше?

### В будущем можно добавить:
- 📊 Веб-интерфейс для просмотра всех КП
- 📧 Автоматические напоминания о приближающихся сроках
- 📈 Статистику по КП (сколько выполнено, средняя сумма и т.д.)
- 💬 Комментарии к КП
- 📎 Прикрепление дополнительных файлов

## Устранение проблем

### Бот не предлагает сохранить КП
1. Убедитесь, что бот перезапущен после обновления кода
2. Проверьте логи бота

### Ошибка при сохранении
1. Проверьте права на запись в папку проекта
2. Посмотрите логи бота: `tail -f logs/bot_*.log`

### База данных не создаётся
1. Проверьте, что файл `core/kp_db.py` существует
2. Запустите тестовый скрипт: `python3 test_kp_db.py`

## Помощь

Подробная документация: `СОХРАНЕНИЕ_КП_В_БД.md`

---

✨ **Готово!** Теперь все КП, которые вы создаёте через бота, можно сохранять в базу данных для отслеживания статуса выполнения.
