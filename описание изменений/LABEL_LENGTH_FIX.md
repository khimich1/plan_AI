# 🏷️ Исправление длины в метках вторичных резов

**Дата:** 2025-11-04  
**Статус:** ✅ Исправлено

---

## ❌ **Проблема:**

На голубых плитах (вторичные резы) отображалась **неправильная длина**.

**Пример:**
- Реальная длина плиты: 5.6 м
- На голубой метке было: `♻️ ПБ 10-3,2-8п` (длина 1.0 м) ❌
- Должно быть: `♻️ ПБ 56-3,2-8п` (длина 5.6 м) ✅

---

## 🔍 **Причина:**

В коде `visualization.py` (строка 448) метка создавалась с **фиксированной длиной 1.0 м**:

```python
'label': f'♻️ {plate_label(1.0, target_width_mm / 1000.0)}'
                              ↑
                         Жёстко задано 1.0 м!
```

**Почему так произошло:**
1. Шаблон вторичных резов создавался **до** присвоения к конкретной плите
2. На этапе создания шаблона длина плиты **ещё не известна**
3. Поэтому использовалась заглушка `1.0 м`

---

## ✅ **Решение:**

Изменена логика создания меток - теперь метка создаётся **при присвоении** шаблона к конкретной плите, когда длина уже известна.

### **Изменение 1: Шаблон без метки (строки 446-450):**

**Было:**
```python
pattern.append({
    'width': target_width_mm / 1000.0,
    'label': f'♻️ {plate_label(1.0, target_width_mm / 1000.0)}'  # ❌ Длина 1.0
})
```

**Стало:**
```python
pattern.append({
    'width': target_width_mm / 1000.0,
    'width_mm': target_width_mm,  # Сохраняем ширину
    'label': None  # Метка будет создана позже
})
```

### **Изменение 2: Создание метки с реальной длиной (строки 484-493):**

**Было:**
```python
if sec_info and sec_info['used'] < sec_info['qty']:
    secondary_cuts_for_plate = sec_info['pattern'].copy()  # ❌ Копия с неправильной меткой
    sec_info['used'] += 1
```

**Стало:**
```python
if sec_info and sec_info['used'] < sec_info['qty']:
    # Создаём копию с правильными метками
    secondary_cuts_for_plate = []
    for sec_cut_template in sec_info['pattern']:
        secondary_cuts_for_plate.append({
            'width': sec_cut_template['width'],
            'label': f'♻️ {plate_label(length, sec_cut_template["width"])}'  # ✅ Реальная длина!
        })
    sec_info['used'] += 1
```

---

## 📊 **Результат:**

### **До исправления:**
```
Плита 5.6 м:
🟢 ПБ 56-3,2-8п (320 мм)
🔵 ♻️ ПБ 10-3,2-8п (320 мм из остатка 340 мм) ❌ длина 1.0 м
```

### **После исправления:**
```
Плита 5.6 м:
🟢 ПБ 56-3,2-8п (320 мм)
🔵 ♻️ ПБ 56-3,2-8п (320 мм из остатка 340 мм) ✅ длина 5.6 м
```

---

## 🎯 **Проверка:**

Для заказа:
```
Плиты ПБ 56-8,6-8п 9
Плиты ПБ 56-3,2-8п 10
```

**На схеме должно быть:**
- ✅ Голубые плиты с правильными длинами (5.6 м, 6.63 м и т.д.)
- ✅ Метки типа `♻️ ПБ 56-3,2-8п` (не `♻️ ПБ 10-3,2-8п`)

---

## 🎉 **Готово!**

Теперь метки на вторичных резах показывают **реальную длину плиты**! ✅

